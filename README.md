# Coffee Daily Logger

A lightweight daily logging app for a coffee sector SMB — track daily sales, stock, or operational entrie
s through a simple REST API backed by a local SQLite database. To run locally, clone the repo and install
 dependencies with `npm install`, then start the server with `npm start` (runs on `http://localhost:3000`
 by default). To run the test suite, use `npm test`.
